<header class="sky">
	<div class="title">MY PAGE &lt;3</div>
</header>

<style>
	@import url("https://fonts.googleapis.com/css2?family=Patrick+Hand&display=swap");

	.sky {
		position: relative;
		height: 220px;
		background:
			radial-gradient(
				circle at 20% 40%,
				rgba(255, 255, 255, 0.6),
				transparent 40%
			),
			radial-gradient(
				circle at 60% 30%,
				rgba(255, 255, 255, 0.5),
				transparent 45%
			),
			radial-gradient(
				circle at 80% 50%,
				rgba(255, 255, 255, 0.4),
				transparent 50%
			),
			linear-gradient(to bottom, #b7dcff, #eaf6ff);

		/* blur only at the bottom */
		-webkit-mask-image: linear-gradient(
			to bottom,
			black 0%,
			black 65%,
			transparent 100%
		);
		mask-image: linear-gradient(
			to bottom,
			black 0%,
			black 65%,
			transparent 100%
		);
	}

	.title {
		position: absolute;
		inset: 0;
		display: grid;
		place-items: center;

		font-family: "Patrick Hand", cursive;
		font-size: clamp(2.5rem, 6vw, 4rem);
		color: #ff69b4; /* pink crayon */

		text-shadow:
			1px 1px 0 rgba(255, 255, 255, 0.6),
			-1px -1px 0 rgba(0, 0, 0, 0.05);

		letter-spacing: 0.08em;
		pointer-events: none;

		/* JITTER + ROUGHNESS */
		transform: rotate(-2deg);
		filter: saturate(1.1);
	}
</style>

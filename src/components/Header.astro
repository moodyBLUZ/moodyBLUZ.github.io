<header class="sky">
	<div class="title">Alyssia&#39;s Diary</div>
</header>

<style>
	@import url("https://fonts.googleapis.com/css2?family=Patrick+Hand&display=swap");

	.sky {
		position: relative;
		height: 200px;

		background:
    /* horizontal center band */
			linear-gradient(
				to right,
				transparent 0%,
				rgba(255, 255, 255, 0.45) 35%,
				rgba(255, 255, 255, 0.65) 50%,
				rgba(255, 255, 255, 0.45) 65%,
				transparent 100%
			),
			/* base sky */
			linear-gradient(to bottom, #b7dcff 0%, #eaf6ff 100%);

		/* fade out bottom into body */
		-webkit-mask-image: linear-gradient(
			to bottom,
			black 0%,
			black 70%,
			transparent 100%
		);
		mask-image: linear-gradient(
			to bottom,
			black 0%,
			black 70%,
			transparent 100%
		);
	}

	.title {
		position: absolute;
		inset: 0;
		display: grid;
		place-items: center;

		font-family: "Patrick Hand", cursive;
		font-size: clamp(2.5rem, 6vw, 4rem);
		color: #f5b4e1;
		text-shadow:
			0 1px 0 rgba(255, 255, 255, 0.55),
			0 4px 2px rgba(0, 0, 0, 0.18);

		letter-spacing: 0.08em;
		pointer-events: none;

		/* JITTER + ROUGHNESS */
		transform: rotate(-2deg);
		filter: saturate(1.1);
	}
</style>
